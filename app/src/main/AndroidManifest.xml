<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.application.demo">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".base.BaseUseActivity" />
        <activity android:name=".high.HighUseActivity" />
        <activity android:name=".base.grid.GridLayoutActivity" />
        <activity android:name=".base.horizontal.HorizontalLayoutActivity" />
        <activity android:name=".base.vertical.VerticalLayoutActivity" />
        <activity android:name=".base.waterfall.WaterfallLayoutActivity" />
        <activity android:name=".high.itemanimator.ItemAnimatorActivity" />
        <activity android:name=".high.itemdecoration.ItemDecorationActivity" />
        <activity android:name=".high.itemtouchhelper.ItemTouchHelperActivity" />
        <activity android:name=".high.snaphelper.SnapHelperActivity" />
        <activity android:name=".high.diffutil.DiffUtilActivity" />
        <activity android:name=".high.diffutil.AsyncDiffUtilActivity" />
        <activity android:name=".high.diffutil.ListAdapterActivity" />
    </application>

</manifest>